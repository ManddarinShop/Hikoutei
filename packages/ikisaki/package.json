{
  "name": "@hikoutei/ikisaki",
  "version": "0.1.3",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ManddarinShop/Hikoutei.git"
  },
  "description": "Hikoutei consistency queue: a durable, ordered, idempotent delivery queue bound to the business transaction.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "typecheck:test": "tsc -p tsconfig.test.json"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
