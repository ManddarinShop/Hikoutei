# 테스트 정리 작업 지침 (test-cleanup-audit)

> 상태: **탐색 단계만 진행 중** — 수정 워커 실행은 사용자 승인 후 시작.

## 1. 목표

**현재 제품에 필요한 동작과 안전성은 보존하고, 과거 구현이나 폐기된 요구사항만 유지시키는 테스트를 제거한다.**

- 테스트 개수·줄 수·커버리지 감소를 목표로 삼지 않는다.
- 기존 테스트를 모두 정당한 요구사항으로 취급하지 않는다.
- 반대로 오래됐거나 내부 구현을 검사한다는 이유만으로 삭제하지 않는다.
- **삭제 근거가 불충분하면 보류한다.**

## 2. 작업 역할

- **메인 에이전트:** 지침 관리, 워커 분담, 요약 취합, 사용자 승인 요청. 소스 직접 조사·수정은 하지 않는다.
- **조사 워커:** 담당 범위를 읽기 전용으로 조사하고 삭제 후보와 근거를 제출한다.
- **수정 워커:** 승인된 후보만 삭제·통합한다. (미기동)
- **리뷰어:** 필요한 검증까지 제거했는지 독립적으로 확인한다. (미기동)
- **검증 워커:** 변경 내역과 테스트·타입체크·빌드 결과를 확인한다. (미기동)

조사 워커에게 삭제 할당량을 주지 않는다. **"삭제할 것 없음"도 정상적인 결과다.**

## 3. 삭제·통합 후보 기준

다음 중 하나에 해당하고, 필요한 검증을 잃지 않는다는 근거가 있어야 한다.

| 분류 | 후보 조건 |
|---|---|
| 폐기된 요구사항 | 지원 종료가 확인된 기능이나 계약만 검사한다. |
| 구현 고정 | 의미 있는 동작·안전성 차이 없이 특정 구현 방식을 강제한다. |
| 중복 검증 | 동일한 조건·실패 유형을 다른 테스트가 실질적으로 검증한다. |
| 무의미한 검증 | 실제 동작 대신 목 설정이나 테스트 내부에서 만든 값만 확인한다. |
| 과거 호환성 | 더는 유지할 필요가 없다고 승인된 호환 동작만 검사한다. |

**주의:** 정상 경로가 같다고 중복은 아니다. 재시도·실패·동시성 조건이 다르면 별도 검증일 수 있다.

## 4. 삭제 근거로 인정하지 않는 것

- 오래된 테스트다.
- 파일이 크거나 실행이 느리다.
- 목을 사용한다.
- 리팩터링 후 실패한다.
- 내부 함수나 비공개 타입을 검사한다.
- 프로덕션 함수가 테스트에서만 직접 호출된다.
- 문서에 해당 기능 설명이 없다.
- 삭제해도 나머지 테스트가 통과한다.

이 사실들은 조사 단서일 뿐, 삭제 증거가 아니다.

## 5. 반드시 보호할 검증

다음 항목의 검증은 **동등한 대체 검증 없이 제거하지 않는다.**

- 현재 지원하는 공개 API와 사용자 시나리오
- SQLite 권위 모델과 local-only 동작
- 엔티티·canonical·outbox의 원자적 커밋과 롤백
- 재시도·중복 처리·재시작 복구
- 원격 수정 감지·충돌·오래된 쓰기 방지
- 스키마 드리프트·타입 변환·잘못된 외부 입력
- 공개 API 노출 제한과 실제로 필요한 의존 경계

공개 API 테스트나 회귀 테스트를 없애려면, 기존 사용자가 의존하는 계약인지와 원래 방지하던 문제가 무엇인지 먼저 확인한다.

## 6. 조사 워커의 제출 형식

파일 전체가 아니라 **테스트 케이스 단위**로 제출한다.

```text
대상: 파일 경로 / 테스트 이름 / 위치
판정: 유지 | 통합 | 삭제 후보 | 판단 보류
현재 검증하는 약속:
불필요하다고 판단한 근거:
관련 프로덕션 경로:
삭제 시 놓칠 수 있는 실패:
남아 있는 동등 검증: 정확한 테스트 이름 또는 없음
필요한 제품 판단:
```

"이 테스트 때문에 기능이 추가됐다"고 주장하려면 변경 이력 등 근거를 제시한다. 현재 함께 존재한다는 사실만으로 인과관계를 단정하지 않는다.

## 7. 실행 순서

1. **현재 유지할 제품 계약을 확인한다.** 문서·구현·테스트가 충돌하면 워커가 임의로 결정하지 않고 보고한다.
2. 검증 워커가 기존 테스트·타입체크·빌드의 기준선을 기록한다. (미기동)
3. 조사 워커들을 독립된 범위로 나누어 병렬 조사한다. 이 단계에서는 수정하지 않는다. ← **현재 단계**
4. 메인이 요약을 취합하고, 리뷰어가 후보의 삭제 근거와 검증 공백을 검토한다.
5. **사용자가 삭제 대상 목록을 승인한 뒤** 수정 워커가 작은 묶음으로 반영한다.
6. 수정 워커 → 리뷰 → 수정 → 재리뷰를 반복하고, 검증 워커가 공식 검증을 실행한다.

기존 실패를 없애기 위해 테스트를 삭제하거나 assertion을 약화시키지 않는다.

## 8. 탐색 워커 범위 분담

| 워커 | 담당 테스트 범위 |
|---|---|
| W1 | 제품 계약 추출 (문서·공개 API·보호 대상 기준선) — 테스트 조사 아님 |
| W2 | `packages/core/contracts`, `packages/core/composition` 테스트 |
| W3 | `packages/core/storage` 테스트 |
| W4 | `packages/core/sync-engine` 테스트 |
| W5 | `packages/cloud/sheets`, `packages/cloud/google-auth` 테스트 |
| W6 | `packages/cloud/cli`, `packages/mcp` 테스트 |
| W7 | `packages/protocol/ikisaki` 테스트 |
| W8 | 루트 `test/` 테스트 |

## 9. 범위와 완료 조건

- 이번 범위는 테스트 정리다. 프로덕션 기능 제거는 별도 제안·승인을 받는다.
- 테스트 삭제가 자동으로 라이브러리 단순화를 의미하지는 않는다. 제거 가능한 프로덕션 코드가 발견되면 후속 후보로 기록한다.
- 탐색 단계에서 테스트 실행은 기준선 파악 목적으로만 허용하며, 코드·문서·Git 상태를 수정하지 않는다.
- 완료 보고에는 **삭제한 의무, 보존한 안전망, 실제 검증 결과, 남은 불확실성**을 적는다.

**최종 원칙: 테스트를 덜 통과시키는 작업이 아니라, 앞으로도 지켜야 할 약속을 정확하게 남기는 작업이다.**