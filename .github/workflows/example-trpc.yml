name: Example tRPC verification

# Runs the tRPC example against an exact published hikoutei version:
# dispatched automatically by the publish workflows (event type
# `examples-verify`) or manually via workflow_dispatch with a version input.
# The scenario itself lives in scripts/ci/examples/trpc.mjs.
on:
  repository_dispatch:
    types: [examples-verify]
  workflow_dispatch:
    inputs:
      version:
        description: "hikoutei version to verify (npm dist-tag or exact version)"
        required: false
        default: latest

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Resolve version
        id: version
        shell: bash
        run: |
          version="${{ github.event.client_payload.version || github.event.inputs.version || 'latest' }}"
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Install example dependencies with published hikoutei
        run: |
          cd examples/trpc
          npm install --no-audit --no-fund --ignore-scripts "hikoutei@${{ steps.version.outputs.version }}"

      - name: Run tRPC scenario
        run: node scripts/ci/examples/trpc.mjs --dir examples/trpc --output "$RUNNER_TEMP/result.json"

      - name: Upload scenario result
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: example-trpc-result
          path: ${{ runner.temp }}/result.json
          if-no-files-found: ignore
